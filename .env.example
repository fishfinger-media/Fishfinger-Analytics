PLAUSIBLE_API_KEY=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
# For local dev, set this to your public base URL (e.g. from a tunnel) so Slack OAuth redirect works.
APP_BASE_URL=

# Local dev only: file storage fallback path (optional)
DEV_STORAGE_PATH=.dev-storage.json
